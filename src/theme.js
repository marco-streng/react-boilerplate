const colors = {
  black: '#000000',
  company: '#38B8BA',
  white: '#ffffff'
}

export default {
  palette: {
    backgroundColor: colors.white,
    textColor: colors.black,
    primaryColor: colors.company,
    header: {
      backgroundColor: colors.company,
      textColor: colors.white
    }
  }
}
